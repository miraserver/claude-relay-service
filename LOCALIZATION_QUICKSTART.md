# 🚀 Локализация - Быстрый старт

## ✅ Статус: Основная функциональность готова!

**Что уже работает:**
- Переключение языка EN/RU ✅
- Страница входа ✅
- Дашборд пользователя ✅
- Страница статистики API ✅
- Ввод API ключей ✅

---

## 🎯 Быстрые команды (Windows)

### Проверить непереведенные строки
```bash
# Вариант 1: Node.js
node scripts/find-untranslated.js

# Вариант 2: Двойной клик
scripts\quick-check.bat
```

### Обновить от upstream
```powershell
# Вариант 1: PowerShell
powershell -ExecutionPolicy Bypass -File scripts\update-from-upstream.ps1

# Вариант 2: Двойной клик
scripts\update-upstream.bat
```

### Установить git hooks (автопроверка)
```bash
node scripts/setup-hooks.js
```

---

## 📁 Структура локализации

```
web/admin-spa/src/
├── locales/
│   ├── index.js          # Конфигурация i18n
│   ├── en/               # Английские переводы
│   │   ├── common.js     # Кнопки, термины
│   │   ├── auth.js       # Аутентификация
│   │   ├── apistats.js   # Статистика API
│   │   ├── user.js       # Пользователь
│   │   └── errors.js     # Ошибки
│   └── ru/               # Русские переводы
│       └── (аналогично)
│
├── components/
│   └── common/
│       └── LanguageSelector.vue  # Переключатель языка
│
└── views/
    ├── UserLoginView.vue         # ✅ Готово
    ├── ApiStatsView.vue          # ✅ Готово
    └── UserDashboardView.vue     # ✅ Готово
```

---

## 🔧 Как добавить перевод

### 1. Найти непереведенную строку
Запустить: `node scripts/find-untranslated.js`

Пример вывода:
```
📄 components/apistats/StatsOverview.vue
Line 134: 使用统计概览
  → Found: 使用统计概览
```

### 2. Добавить ключ перевода

**en/apistats.js:**
```javascript
export default {
  overview: {
    usageStatsTitle: 'Usage Statistics Overview'
  }
}
```

**ru/apistats.js:**
```javascript
export default {
  overview: {
    usageStatsTitle: 'Обзор статистики использования'
  }
}
```

### 3. Использовать в компоненте

**Template:**
```vue
<h3>{{ $t('apistats.overview.usageStatsTitle') }}</h3>
```

**Script (если нужен t()):**
```vue
<script setup>
import { useI18n } from 'vue-i18n'
const { t } = useI18n()

const title = t('apistats.overview.usageStatsTitle')
</script>
```

### 4. Проверить
```bash
npm run dev
# Открыть http://localhost:5173
# Переключить язык в интерфейсе
```

---

## 📊 Прогресс

| Компонент | Статус |
|-----------|--------|
| **Критические страницы** | |
| UserLoginView.vue | ✅ 100% |
| ApiStatsView.vue | ✅ 100% |
| UserDashboardView.vue | ✅ 100% |
| **Компоненты API Stats** | |
| ApiKeyInput.vue | ✅ 100% |
| StatsOverview.vue | ⚠️ 60% |
| TokenDistribution.vue | ❌ 0% |
| LimitConfig.vue | ❌ 0% |
| ModelUsageStats.vue | ❌ 0% |
| AggregatedStatsCard.vue | ❌ 0% |
| **Компоненты User** | |
| UserApiKeysManager.vue | ❌ 0% |
| CreateApiKeyModal.vue | ❌ 0% |
| ViewApiKeyModal.vue | ❌ 0% |
| UserUsageStats.vue | ❌ 0% |

**Итого:** ~50% основного функционала

---

## 🎓 Примеры использования

### Простой текст
```vue
<p>{{ $t('common.labels.loading') }}</p>
```

### С переменными
```vue
<p>{{ $t('user.welcome', { name: userName }) }}</p>
```

Ключ:
```javascript
welcome: 'Welcome, {name}!'
```

### Условный текст
```vue
<span>{{ isActive ? $t('apistats.overview.active') : $t('apistats.overview.inactive') }}</span>
```

### В computed
```vue
<script setup>
import { computed } from 'vue'
import { useI18n } from 'vue-i18n'

const { t } = useI18n()
const buttonText = computed(() =>
  loading.value ? t('apistats.input.querying') : t('apistats.input.queryButton')
)
</script>
```

---

## 🛠️ Инструменты

### find-untranslated.js
**Что делает:**
- Сканирует все Vue/JS/TS файлы
- Находит китайский текст
- Исключает уже переведенное ($t())
- Показывает файл и номер строки

**Пример:**
```bash
node scripts/find-untranslated.js
```

### update-from-upstream.ps1
**Что делает:**
1. Fetch изменений от оригинального репозитория
2. Показывает что будет обновлено
3. Stash локальных изменений
4. Merge upstream
5. Restore stashed изменений
6. Проверяет непереведенные строки

**Пример:**
```powershell
# Просмотр без изменений
powershell -ExecutionPolicy Bypass -File scripts\update-from-upstream.ps1 -DryRun

# Реальное обновление
powershell -ExecutionPolicy Bypass -File scripts\update-from-upstream.ps1
```

### setup-hooks.js
**Что делает:**
- Устанавливает pre-commit hook
- Автоматически проверяет непереведенные строки перед каждым коммитом
- Блокирует коммит если найдены проблемы

**Пример:**
```bash
node scripts/setup-hooks.js
```

**Обход (когда нужно):**
```bash
git commit --no-verify -m "WIP: incomplete translation"
```

---

## 📖 Полная документация

- **LOCALIZATION_COMPLETE.md** - Детальное описание всего что сделано
- **LOCALIZATION_PLAN.md** - Полный план локализации
- **UPDATE_GUIDE.md** - Руководство по обновлению от upstream

---

## ⚡ Следующие шаги

1. **Завершить StatsOverview.vue** (осталось 40%)
2. **Локализовать TokenDistribution.vue**
3. **Локализовать ModelUsageStats.vue**
4. **Локализовать LimitConfig.vue**
5. **Локализовать AggregatedStatsCard.vue**

Каждый компонент:
```bash
# 1. Найти непереведенные строки
node scripts/find-untranslated.js

# 2. Добавить ключи в en/ru файлы
# 3. Заменить текст на $t()
# 4. Проверить в браузере
```

---

## ✨ Готово к использованию!

Основной функционал локализован. Пользователи уже могут:
- Переключать язык
- Работать со статистикой
- Управлять дашбордом
- Входить в систему

**Все инструменты для продолжения готовы!** 🎉
