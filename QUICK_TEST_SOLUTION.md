# 🚀 Быстрое решение для тестирования User Dashboard

Для полноценного backend нужен Redis, который не установлен. Но есть два быстрых решения:

## ✅ Решение 1: Установить Redis (занимает 5 минут)

### Для Windows:

1. **Скачайте Redis для Windows:**
   - https://github.com/microsoftarchive/redis/releases
   - Скачайте `Redis-x64-3.0.504.msi`

2. **Установите** (двойной клик, Next-Next-Next)

3. **Redis запустится автоматически как служба Windows**

4. **Проверьте:**
   ```bash
   redis-cli ping
   ```
   Должно ответить: `PONG`

### После установки Redis:

```bash
cd C:\Users\user\Downloads\claude-relay-service

# Установить зависимости backend (если не установлены)
npm install

# Инициализация (создаст .env, config, admin аккаунт)
npm run setup

# Запустить backend
npm start
```

Backend запустится на `http://localhost:3001`

---

## ✅ Решение 2: Временно отключить auth (БЕЗ Redis, БЫСТРО)

Я могу временно изменить код, чтобы страница `/user` открывалась без авторизации.

### Что я сделаю:

1. Отключу проверку токена в `UserDashboardView.vue`
2. Добавлю mock данные пользователя
3. Страница откроется напрямую без логина

### Преимущества:
- ✅ Не нужен Redis
- ✅ Не нужен backend
- ✅ Работает сразу
- ✅ Можно тестировать всю локализацию

### Недостатки:
- ⚠️ Временное изменение кода (легко откатить)
- ⚠️ Только для теста локализации

---

## 🤔 Что выбрать?

**Если хотите только протестировать локализацию СЕЙЧАС:**
→ Выберите **Решение 2** (я отключу auth за 1 минуту)

**Если планируете использовать проект полноценно:**
→ Выберите **Решение 1** (установите Redis)

---

## 📝 Что вы протестируете на /user

После любого из решений:

1. Откроете `http://localhost:5173/user`
2. Увидите полностью локализованный интерфейс:
   - Навигация (Overview, API Keys, Usage Stats, Tutorial)
   - Приветствие (Welcome, [username])
   - 5 статистических карточек
   - Информация об аккаунте
   - Кнопка Logout
3. Переключите язык EN → RU
4. Весь текст изменится на русский

**Какое решение предпочитаете?**
